node_modules/
.pnpm-store/
.release/
__pycache__/
dist/
build/
coverage/
.turbo/
.env
.env.local
.env.*.local
*.log
.DS_Store
.firebase/

# One-off data-migration inputs (real member/financial data) — never committed.
apps/backend/scripts/data/

# KTP photo storage (STORAGE_PATH default) — local dev/test runtime output,
# never source. Both the internal upload-ktp route and the public
# self-registration endpoint write here.
apps/backend/uploads/
